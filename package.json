{
  "name": "fontsovertime",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "crawl": "node crawler/crawl.ts",
    "typecheck": "tsc",
    "merge": "node pipeline/merge.ts",
    "aggregate": "node pipeline/aggregate.ts",
    "backfill": "node pipeline/wayback.ts",
    "dev": "pnpm --filter web dev",
    "build": "node pipeline/aggregate.ts && pnpm --filter web build",
    "test": "node --test pipeline/",
    "lists": "node pipeline/indie.ts && node pipeline/sitelists.ts",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "playwright": "^1.63.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.136.3"
  },
  "packageManager": "pnpm@10.28.0"
}
